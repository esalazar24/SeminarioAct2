/*
export class Anteproy {
  public nombre!: string;
  public detalle!: string;
  public estado!: string;
  public items: any[] = [];
  public id: any;

  setValores(item: any): void {
    this.id = item.id;
    this.nombre = item.nombre;
    this.detalle = item.detalle;
    this.estado = item.estado;
    this.items = item.items || [];
  }
}
*/

export class Anteproy {
  public nombre!: string;
  public detalle!: string;
  public estado!: string;
  public items: any[] = [];
  public id: any;

  setValores(item: any): void {
    this.id = item.id;
    this.nombre = item.nombre;
    this.detalle = item.detalle;
    this.estado = item.estado;
    this.items = item.items || [];
  }
}
